PROJECT(lasagne)
CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
include_directories(kernel/include)
FILE(GLOB_RECURSE CFILES *.c)
FILE(GLOB_RECURSE HFILES *.h)
FILE(GLOB_RECURSE SFILES *.asm)
ADD_EXECUTABLE(lasagne ${CFILES} ${HFILES} ${SFILES})